# a function that takes a list of integers, and returns a new list with every other element from the original list, starting with the first element. 
# For instance: every_other([1,4,7,2,5]) # => [1,7,5]

START 

# LIST OF INTEGERS (my_list)

SET empty list 

FOR LOOP over my_list in a range(0, -1, 1) 
    FOR index over my_list: will add numbers skipping 1 number
    add index to empty list:

    PRINT empty list(that isnt empty anymore :p)
